{% extends 'base.html.twig' %}

{% block title %}Edit Post - {{ association.name }}{% endblock %}

{% block body %}
    <h1>Edit Post for {{ association.name }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Update Post</button>
    {{ form_end(form) }}

    <a href="{{ path('app_association_post_show', {'id': association.id, 'postId': post.id}) }}" class="btn btn-secondary mt-3">Back to Post</a>
    <a href="{{ path('app_association_posts', {'id': association.id}) }}" class="btn btn-secondary mt-3">Back to Posts</a>
{% endblock %}